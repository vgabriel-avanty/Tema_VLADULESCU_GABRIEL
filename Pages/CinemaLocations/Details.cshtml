@page
@model Tema_VLADULESCU_GABRIEL.Pages.CinemaLocations.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>CinemaLocation</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CinemaLocation.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CinemaLocation.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.CinemaLocation?.ID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
