@page
@model Tema_VLADULESCU_GABRIEL.Pages.CinemaLocations.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>CinemaLocation</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CinemaLocation.Location" class="control-label"></label>
                <input asp-for="CinemaLocation.Location" class="form-control" />
                <span asp-validation-for="CinemaLocation.Location" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CinemaLocation.CinemaID" class="control-label"></label>
                <select asp-for="CinemaLocation.CinemaID" class ="form-control" asp-items="ViewBag.CinemaID"></select>
            </div>
            <div class="form-group">
                <label asp-for="CinemaLocation.CountyID" class="control-label"></label>
                <select asp-for="CinemaLocation.CountyID" class ="form-control" asp-items="ViewBag.CountyID"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
