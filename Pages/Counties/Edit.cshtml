@page
@model Tema_VLADULESCU_GABRIEL.Pages.Counties.EditModel

@{
    ViewData["Title"] = "Editează Județ";
}

<a asp-page="Index" class="btn btn-primary">Înapoi</a>
<h1 class="d-flex justify-content-center mt-5">Editează Județ</h1>

<div class="row">
    <div class="col-md-4 m-auto">
        <form method="post" class="shadow p-5">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="County.ID" />
            <div class="form-group">
                <label asp-for="County.Name" class="control-label"></label>
                <input asp-for="County.Name" class="form-control" />
                <span asp-validation-for="County.Name" class="text-danger"></span>
            </div>
            <div class="form-group mt-3">
                <input type="submit" value="Save" class="btn btn-primary w-100" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
