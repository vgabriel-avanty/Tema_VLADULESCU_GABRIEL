@page
@model Tema_VLADULESCU_GABRIEL.Pages.Counties.IndexModel

@{
    ViewData["Title"] = "Listă Județe";
}

<h1>Listă Județe</h1>

<div class="d-flex justify-content-end">
  <a class="btn btn-primary" asp-page="Create">Adaugă Județ</a>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.County[0].Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.County) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td style="display: flex;justify-content:end;">
                <div>
                   <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                   <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
                </div>
            </td>
        </tr>
}
    </tbody>
</table>
